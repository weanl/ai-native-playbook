本 PR 收尾自检。严格逐项打勾（✅）或打叉（❌），禁止含糊。

## 范围与依赖

[ ] **范围未越界**：贴 `git diff main --name-only` 输出；逐个文件标注是否在 @docs/PLAN.md 本 PR 范围内
[ ] **依赖未越界**：贴 `git diff main -- pyproject.toml`；新增依赖均已在 PLAN 声明
[ ] **core/ 未改既有抽象**：贴 `git diff main -- src/nextaiops_algo/core/` 文件级摘要；既有文件改动须有 ADR 支撑

## 红线满足（R1~R7）

[ ] R1 稳定/可变分离：列出涉及 core/ 的改动证据
[ ] R2 算法接入契约：若涉及算法，列出 Algorithm 协议 + REGISTRY 注册 + Table I/O 证据
[ ] R3 复现性：若涉及实验/落库，列出 run 字段完整性证据 + 复现性测试结果
[ ] R4 冒烟覆盖：若涉及算法或 pipeline，列出 smoke 通过证据
[ ] R5 测试纪律：未改断言 / 未吞异常 / 未 skip（贴关键测试片段）
[ ] R6 范围纪律：与上文「范围未越界」一致
[ ] R7 范围外不做：未引入 MLflow / 在线推理 / 流式 / AutoML 等

## 验收

[ ] **PR 验收线全部达成**：逐条对照 @docs/PLAN.md 本 PR 段落，给证据
[ ] **新增/修改逻辑都有测试或冒烟覆盖**
[ ] **静态检查全绿**：贴 `make lint` 最后输出
[ ] **测试全绿**：贴 `make test` 最后输出

## PR 描述

[ ] 已草拟 PR 描述：动机 / 范围 / 影响面 / 验证方式 / 已知遗留 / 自检报告

---

任一项 ❌ → 停下报告，**不要自行修复**。等我指示。
