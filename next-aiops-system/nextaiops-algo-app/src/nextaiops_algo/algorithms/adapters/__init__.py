"""Algorithm adapters - bridge external algorithm libraries to AnomalyDetector protocol.

This package provides adapter classes that wrap third-party anomaly detection
implementations (e.g., TSB-UAD) to conform to the project's AnomalyDetector
protocol, enabling seamless integration through the REGISTRY mechanism.
"""
